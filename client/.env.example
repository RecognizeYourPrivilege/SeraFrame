# Copy to `.env.development.local` to override the committed dev default.
# `true` (default): MSW serves contract v1 so `npm run dev` works without BACK.
# `false`: the Vite dev server proxies `/api` to http://127.0.0.1:8081.
VITE_USE_MOCKS=true
